[build]
  base = "Frontend/vite-project"
  command = "npm run build"
  publish = "dist"

# React Router SPA fallback (so /login, /signup, etc. work on refresh)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "22"
